## 2026-05-14 (김준호)

- 자바 스프링 프로젝트 첫 푸쉬

## 2026-05-15 (곽창모)

- healthCheck endpoint 작성

## 2026-05-15 (곽창모)

- server memory 엔드포인트 구현
- 기존 healthCheck 엔드포인트 -> server/Status.java 로 변경 및 Check, Memory 엔드포인트를 나눴음

## 2026-06-18 (곽창모)

- auth 로그인/회원가입 테스트 API 구현
- auth.controller 패키지 생성 후 Login, Register 엔드포인트를 분리했음
- Login 엔드포인트는 POST /auth/login 으로 구현하고, 테스트 계정 test@test.com / 1234 기준으로 로그인 성공/실패 응답을 반환하도록 작성함
- Register 엔드포인트는 POST /auth/register 으로 구현하고, 이메일/비밀번호를 받아 Postman에서 요청/응답 테스트가 가능하도록 작성함
- auth.dto 패키지를 생성하고 LoginRequest, LoginResponse, RegisterRequest DTO를 분리했음
- ERD 기준으로 src/main/resources/db/poke_schema.sql 파일을 추가하고 POKE_USER, POKE_SHOPS, POKE_REVIEWS 등 DB 테이블 생성 SQL을 작성했음
- RegisterRequestTest 테스트 파일을 추가했음

## 2026-06-18 (곽창모)

- auth DTO Lombok 적용 및 pom.xml 수정
- pom.xml에 Lombok 의존성을 추가했음
- LoginRequest, LoginResponse, RegisterRequest DTO를 Lombok 어노테이션 기반으로 변경했음
- 기존 getter/setter/생성자 코드를 제거하여 DTO 코드를 간소화했음

## 2026-07-02 (곽창모)

- auth/controller 패키지를 auth/AuthController로 변경했음
- Login.java에서 직접 처리하던 테스트 로그인 검증 로직을 UserService.login()으로 분리했음
- Register.java에서 직접 생성하던 회원가입 테스트 응답 로직을 UserService.register()로 분리했음
- Login.java, Register.java는 요청을 받고 UserService를 호출하는 역할만 하도록 구조를 변경했음
- 회원가입 테스트 응답에서 password 반환을 제거했음
- UserService.java를 추가하고 로그인, 회원가입, 로그아웃, 인증코드 발송, 비밀번호 재설정 테스트 메서드를 작성했음
- Logout.java를 추가하여 POST /auth/logout 테스트 엔드포인트를 구현했음
- SendVerifyCode.java를 추가하여 POST /auth/sendVerifyCode 테스트 엔드포인트를 구현했음
- ResetPassword.java를 추가하여 POST /auth/resetPassword 테스트 엔드포인트를 구현했음
- SendVerifyCodeRequest, ResetPasswordRequest DTO를 추가했음

## 2026-07-07 (김준호)

- scheduler/entity/newsEntity, scheduler/infos/News.java scheduler/repository/NewsRepository.java config/RestTemplateConfig.java 생성
- config/RestTemplateConfig은 외장 라이브러리인 RestTemplate를 지속적으로 new 하는것을 방지하기위해 콘픽으로 줄수있도록 구현
- 현재 ERD에 따라 포켓몬 news정보 오라클에 올리는 로직 구현

## 2026-07-08 (김준호)

- 스케쥴러 로직 수정 
- scheduler/NewsEntity.java newsDate 추가
- .env / Dotenv 추가

## 2026-07-09 (김준호)

- 스케쥴러 로직 수정 
- 뉴스 스케쥴러 테스트 까지 완료

## 2026-07-13 (김준호)

- 스케쥴러와 서버 구분으로 인한 스케쥴러 코드 전체 이동 및 제거 완료
- 기존 java/com/example/server -> java/server로 불필요한 경로 제거
- java/server/config/CORS.java 로 프론트간 CORS 에러 해결
- java/server/data/dto,entity,repository java/server/api.java 임시 엔드포인트 구현 완료 <- 청하님이랑 태준님 엔드포인트 구현할 때 되면 지울예정

## 2026-07-14 (곽창모)

- application.yml에 Oracle JDBC 드라이버 설정을 추가했음
- userEntity를 Oracle POKE_USER 테이블 구조에 맞게 변경했음
- userEntity의 테이블 매핑명을 POKE_USER로 명시했음
- userEntity의 기본키 타입을 Long에서 String으로 변경하고 기존 시퀀스 자동 생성 설정을 제거했음
- userEntity에서 DB에 존재하지 않는 img 필드를 제거했음
- 기존 create_at 문자열 필드를 CREATED_AT 컬럼과 연결되는 LocalDateTime 타입의 createdAt 필드로 변경했음
- CREATED_AT 값은 Oracle의 DEFAULT SYSTIMESTAMP를 사용하도록 설정했음
- userRepository의 기본키 타입을 Long에서 String으로 변경했음
- 회원가입 이메일 중복 확인을 위한 existsByEmail 메서드를 추가했음
- 로그인 회원 조회를 위한 findByEmail 메서드를 추가했음
- RegisterRequest에 회원 이름을 전달받기 위한 name 필드를 추가했음
- pom.xml에 spring-security-crypto 의존성을 추가했음
- PasswordConfig를 추가하고 BCryptPasswordEncoder를 PasswordEncoder Bean으로 등록했음
- UserService.register()의 테스트 응답 로직을 Oracle POKE_USER 테이블에 실제 회원 정보를 저장하는 방식으로 변경했음
- 회원가입 시 이메일을 소문자로 정리하고 중복 이메일을 확인하도록 구현했음
- 회원가입 시 UUID를 이용하여 24자리 회원 ID를 생성하도록 구현했음
- 회원가입 비밀번호를 BCrypt로 암호화하여 저장하도록 구현했음
- UserService.login()의 고정 테스트 계정 검증 로직을 제거했음
- 로그인 시 이메일로 POKE_USER 회원 정보를 조회하고 BCrypt 비밀번호를 비교하도록 변경했음
- 로그인 성공 시 HttpSession에 로그인 회원 이메일을 저장하도록 Login.java를 변경했음
- Logout.java의 테스트 응답 로직을 제거하고 기존 세션을 invalidate하여 실제 로그아웃하도록 변경했음
- Oracle POKE_USER 테이블에서 사용하지 않는 CREATE_AT, IMG 컬럼을 제거했음
- poke_schema.sql에서 현재 사용하지 않는 중복 POKE_NEWS 테이블 생성 SQL을 삭제했음
- Postman과 SQL Developer를 통해 회원가입 DB 저장, 비밀번호 암호화, 로그인 성공·실패 및 로그아웃 동작을 확인했음
- 로그인 세션을 확인하기 위한 GET /auth/status 엔드포인트를 추가했음

## 2026-07-15 (곽창모)

- pom.xml에 요청값 검증을 위한 spring-boot-starter-validation 의존성을 추가했음
- RegisterRequest에 이메일, 비밀번호, 이름의 필수 입력·형식·길이 검증을 추가했음
- LoginRequest에 이메일과 비밀번호의 필수 입력·형식·길이 검증을 추가했음
- Login.java와 Register.java에 @Valid를 적용하여 잘못된 요청값이 Service로 전달되지 않도록 변경했음
- server.exception 패키지를 생성하고 GlobalExceptionHandler를 추가했음
- 회원가입과 로그인 검증 실패 시 필드별 오류 내용을 HTTP 400 JSON 응답으로 반환하도록 구현했음
- DuplicateEmailException을 추가하고 중복 이메일 회원가입을 HTTP 409 Conflict로 처리하도록 변경했음
- LoginStatus.java에서 기존 세션이 없을 때 새로운 세션을 생성하지 않도록 변경했음
- CORS 허용 출처를 React 개발 서버 주소로 제한하고 세션 쿠키 전달이 가능하도록 설정했으며, 허용 메서드에 PATCH를 추가했음
- 세션 로그아웃 구현 후 더 이상 사용하지 않는 UserService.logout() 테스트 메서드를 제거했음
- ResetPasswordRequest를 이메일·인증코드 방식에서 현재 비밀번호와 새 비밀번호를 받는 구조로 변경했음
- userEntity에 암호화된 비밀번호를 변경하는 changePassword 메서드를 추가했음
- 로그인 세션의 이메일로 회원을 조회하고 현재 비밀번호를 검증한 뒤 새 비밀번호를 BCrypt로 암호화하여 Oracle DB에 저장하도록 구현했음
- 비로그인 상태의 비밀번호 변경 요청을 HTTP 401 Unauthorized로 차단하도록 구현했음
- 현재 비밀번호가 올바르지 않은 경우 HTTP 400 응답을 반환하도록 처리했음
- 새 비밀번호가 현재 비밀번호와 같은 경우 비밀번호를 변경하지 않도록 처리했음
- 비밀번호 변경 작업에 @Transactional을 적용했음
- 비밀번호 변경 성공 후 기존 로그인 세션을 종료하여 새 비밀번호로 다시 로그인하도록 변경했음
- Postman으로 요청값 검증, 중복 이메일 처리, 세션 로그인 상태, 비밀번호 변경 및 변경 후 자동 로그아웃을 확인했음

## 2026-07-16 (김준호)

- dto/NewsListResponse.java, repository/NewsRepository.java, api.java 응답 시 데이터
  제공 방식을 기존 페이징 방식에서 리스트 형태로 변경
  
## 2026-07-16 (곽창모)

- 매장 리뷰 기능에서 로그인 회원과 리뷰 작성자를 비교할 수 있도록 LoginResponse에 userId 필드를 추가했음
- UserService.login()이 로그인 성공 시 POKE_USER 테이블의 회원 ID를 LoginResponse에 포함하도록 변경했음
- 로그인 실패 시 변경된 LoginResponse 생성자 구조에 맞게 userId와 email을 null로 반환하도록 수정했음
- Login.java에서 로그인 성공 시 기존 LOGIN_USER_EMAIL과 함께 LOGIN_USER_ID를 HttpSession에 저장하도록 변경했음
- 리뷰 외래키 연결에 필요한 최소 매장 테이블인 POKE_SHOPS를 생성했음
- POKE_SHOPS에 SHOP_ID 기본키와 SHOP_NAME 필수 컬럼을 구성했음
- 매장 리뷰 정보를 저장하기 위한 POKE_REVIEWS 테이블을 생성했음
- POKE_REVIEWS에 REVIEW_ID, USER_ID, SHOP_ID, CONTENT, RATING, CREATED_AT 컬럼을 구성했음
- POKE_REVIEWS.REVIEW_ID를 기본키로 설정했음
- POKE_REVIEWS.USER_ID가 POKE_USER.ID를 참조하도록 외래키를 설정했음
- POKE_REVIEWS.SHOP_ID가 POKE_SHOPS.SHOP_ID를 참조하도록 외래키를 설정했음
- 회원 또는 매장 삭제 시 연결된 리뷰도 삭제되도록 외래키에 ON DELETE CASCADE를 적용했음
- 리뷰 평점이 1점부터 5점까지만 저장되도록 CHECK 제약조건을 적용했음
- POKE_SHOPS와 POKE_REVIEWS에 테스트 데이터를 삽입하고 회원·매장·리뷰 외래키 연결을 확인했음
- 리뷰 기능을 기능별로 분리하기 위해 server.review 패키지를 생성했음
- 리뷰 패키지를 controller, dto, entity, repository, service 구조로 구성했음
- ReviewEntity를 추가하고 Oracle POKE_REVIEWS 테이블과 연결했음
- ReviewEntity의 reviewId를 REVIEW_ID 기본키와 매핑했음
- ReviewEntity의 userId와 shopId를 각각 USER_ID, SHOP_ID 컬럼과 매핑했음
- Oracle CLOB 타입의 CONTENT 컬럼을 Java String 타입으로 처리하기 위해 @Lob을 적용했음
- CREATED_AT은 Oracle의 DEFAULT SYSTIMESTAMP 값을 사용하도록 insertable과 updatable을 false로 설정했음
- ReviewRepository를 추가하고 JpaRepository의 기본키 타입을 String으로 설정했음
- 특정 매장의 리뷰를 작성일 최신순으로 조회하는 findByShopIdOrderByCreatedAtDesc 메서드를 추가했음
- 리뷰 삭제 전 URL의 reviewId와 shopId가 모두 일치하는 리뷰를 조회하기 위해 findByReviewIdAndShopId 메서드를 추가했음
- ReviewResponse DTO를 추가하고 리뷰 ID, 회원 ID, 매장 ID, 내용, 평점, 작성일을 반환하도록 구성했음
- 로그인한 회원이 작성한 리뷰인지 구분할 수 있도록 ReviewResponse에 mine 필드를 추가했음
- ReviewResponse.from()을 추가하여 ReviewEntity를 API 응답 DTO로 변환하도록 구현했음
- ReviewService를 추가하고 특정 매장의 리뷰 목록을 최신 작성순으로 조회하도록 구현했음
- 로그인하지 않은 사용자도 리뷰 목록을 조회할 수 있도록 구현했음
- ReviewService.deleteReview()를 추가하고 로그인한 회원이 본인의 리뷰만 삭제할 수 있도록 구현했음
- 비로그인 상태의 리뷰 삭제 요청을 HTTP 401 Unauthorized로 처리했음
- 존재하지 않거나 매장 ID가 일치하지 않는 리뷰 삭제 요청을 HTTP 404 Not Found로 처리했음
- 다른 회원이 작성한 리뷰 삭제 요청을 HTTP 403 Forbidden으로 처리했음
- 리뷰 목록 조회에는 읽기 전용 트랜잭션을 적용하고 리뷰 삭제에는 일반 트랜잭션을 적용했음
- ReviewController를 추가하고 GET /info/shops/{shopId}/reviews 엔드포인트를 구현했음
- ReviewController에 DELETE /info/shops/{shopId}/reviews/{reviewId} 엔드포인트를 구현했음
- 리뷰 목록 조회 시 getSession(false)를 사용하여 비로그인 조회 요청에서 불필요한 새 세션이 생성되지 않도록 처리했음
- 리뷰 삭제 성공 시 HTTP 204 No Content를 반환하도록 구현했음
- 서버 재실행 시 ReviewRepository가 정상적으로 등록되고 Oracle DB 연결과 Spring Boot 실행이 완료되는 것을 확인했음 

## 2026-07-21 오청하
- data 패키지 수정으로 News 조회 가능 [{추가: NewsController, NewsDTO, NewsEntity, NewsRepository, NewsService}, {주석처리: dto_temp, entity_temp, repository_temp 패키지 및 api_temp 파일}]
- .env 파일 추가 (현재는 IP주소를 기준으로 바라보게끔 되어 있음)
- ServerApplication.java 수정 (Page JSON 직렬화 경고 해결용 코드 추가)

## 2026-07-22 김준호
- poke_schema.sql 수정
- poke_schema.sql

## 2026-07-22 오청하
- Shop 틀 만듦
- temp 폴더 제거

## 2026-07-22 김준호
- GlobalExceptionHandler.java, CustomException.java 공통 예외 처리 

## 2026-07-23 오청하
- Shop 중간작업
- ServerApplication 일부 주석 제거

## 2026-07-23 오청하
- GoodsController 틀만 추가
- GoodsDTO 코드 추가
- GoodsEntity 코드 추가
- GoodsRespository 틀만 추가
- GoodsService 틀만 추가

## 2026-07-23 (곽창모)
- 담당 인증 및 리뷰 기능을 팀의 최신 코드 처리 방식에 맞게 정리했음
- 로그인 사용자 정보를 기존 `LOGIN_USER_ID`, `LOGIN_USER_EMAIL` 개별 세션 방식에서 `session["user"]` 하나로 통일했음
- `session["user"]`에는 `id`, `email`, `name`을 저장하고, 인증이 필요한 기능에서는 변경 가능한 이메일 대신 회원 PK인 `id`를 사용자 식별 기준으로 사용하도록 수정했음
- 로그인, 회원가입, 회원정보 변경의 정상 응답은 기존 Auth 코드와 동일하게 각 Service에서 `Map.of(...)`로 직접 반환하도록 정리했음
- 담당 기능의 오류 처리는 `ResponseStatusException` 대신 기존 팀 공통 `CustomException`을 발생시키도록 변경했음
- 회원 이메일과 이름을 변경하는 `POST /auth/update` 기능을 구현했음
- 회원정보 변경 시 `session["user"]`의 회원 `id`로 현재 사용자를 조회하고, 실제로 이메일이 변경되는 경우에만 이메일 중복 여부를 확인하도록 구현했음
- 이메일과 이름은 `userEntity.changeProfile()`을 통해 변경하고, 변경 후 현재 세션의 `id`, `email`, `name`도 최신 회원정보로 갱신하도록 구현했음
- 비밀번호 변경 시 세션의 이메일을 이용해 사용자를 다시 찾던 방식 대신 `session["user"]`의 회원 `id`로 사용자를 조회하도록 수정했음
- 세션이 없거나 정상적인 로그인 사용자 정보가 없는 경우 `UnauthorizedException`으로 처리하도록 통일했음
- 회원가입과 회원정보 변경의 이메일·이름 입력 검증 조건을 동일한 정책으로 맞췄음
- 회원가입 DTO의 기존 이메일 최대 길이와 오류 메시지가 서로 다르던 부분을 수정했음
- 회원가입 및 회원정보 변경은 이메일 최대 40자, 이름 최대 8자 기준을 사용하도록 맞췄음
- 로그인 상태 조회(`/auth/status`)는 기존 프론트의 `data.login` 사용 방식이 깨지지 않도록 기존 응답 구조를 유지했음
- 로그인 상태에서는 `login`, `email`, `name`을 반환하고, 회원 `id`는 외부 응답에 불필요하게 추가하지 않고 세션 내부에서만 사용하도록 정리했음
- 리뷰 목록 조회 및 삭제 기능도 로그인 사용자 확인 시 `session["user"]`의 회원 `id`를 사용하도록 통일했음
- 기존 리뷰 코드의 `LOGIN_USER_ID` 세션 속성 사용을 제거했음
- 리뷰 목록 조회 시 세션의 회원 `id`와 리뷰 작성자의 `userId`를 비교하여 본인 리뷰 여부(`mine`)를 판단하도록 수정했음
- 리뷰 기능에서 사용하던 `ResponseStatusException`을 제거하고 `CustomException` 기반 예외 처리 방식으로 변경했음
- 리뷰 삭제 시 로그인 정보가 없으면 `UnauthorizedException`, 조건에 맞는 리뷰를 찾을 수 없으면 `NotFoundException`을 사용하도록 수정했음
- 리뷰 삭제는 `reviewId`, `shopId`, 로그인 회원 `userId`가 모두 일치하는 리뷰만 조회하도록 Repository 조건을 변경하여 본인 리뷰만 삭제할 수 있도록 처리했음
- 리뷰 Service에서 세션 이메일로 회원을 다시 조회하여 ID를 얻던 불필요한 DB 조회를 제거하고, 세션에 이미 저장된 회원 `id`를 바로 사용하도록 정리했음
- 최신 매장 구조에 맞춰 리뷰의 `shopId` 타입을 `String`에서 `Long`으로 변경했음
- `ReviewEntity`, `ReviewResponse`, `ReviewRepository`, `ReviewService`, `ReviewController`에서 `shopId`를 동일하게 `Long` 타입으로 사용하도록 맞췄음
- `ResetPassword`, `UpdateProfile`에서 한 번만 사용하던 별도 세션 조회 보조 메서드를 제거하고 필요한 세션 검증을 Controller 내부에서 바로 처리하도록 단순화했음

## 2026-07-23 오청하
- Goods 모두 내용 추가 (테스트 필요)
- ShopService 코드 한줄 수정

## 2026-07-23 오청하
- ShopEntity, ShopDTO 변수명 수정

## 2026-07-23 오청하
- ShopEntity, ShopDTO 변수명 수정

## 2026-07-24 오청하
- GoodsDTO 수정

## 2026-08-28 오청하
- NewsController에서 불필요한 코드 제거
- 
## 2026-09-02 오청하
- Localization 완료 (login, logout, register, sessiondebug, updateprofile, loginrequest, registerrequest, resetpasswordrequest, updateprofilerequest, userservice, pokecodexservice, reviewcontroller, reviewdto, reviewservice)